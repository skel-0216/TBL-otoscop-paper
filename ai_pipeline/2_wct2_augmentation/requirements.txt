# Versions match the paper environment (see the repository environment.yml).
# albumentations is pinned: its Affine / ReplayCompose API changed in 1.4+, and
# the recorded per-image replay metadata assumes the 1.3.x behaviour. numpy is
# held below 2.0 for compatibility with albumentations 1.3.1.
torch>=2.0
torchvision>=0.15
numpy>=1.23,<2
pandas>=1.5
Pillow>=9.0
opencv-python==4.11.0.86
albumentations==1.3.1
tqdm>=4.64
